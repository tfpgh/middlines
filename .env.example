# FastAPI admin UI credentials for /api/admin.
MIDDLINES_ADMIN_USERNAME=admin
MIDDLINES_ADMIN_PASSWORD=replace-with-a-strong-admin-password

# FastAPI admin UI session signing secret.
# Generate a strong random value, e.g.: openssl rand -hex 32
MIDDLINES_SESSION_SECRET=replace-with-a-strong-session-secret
